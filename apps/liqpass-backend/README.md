# liqpass-backend (Phase A)

## Regression test for claim pipeline

`scripts/mock_trigger.ts` is the minimal regression driver for the claim pipeline.
Keep it to validate changes such as jp-verify replacement, payout formula updates, or contract upgrades.
